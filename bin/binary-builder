#!/usr/bin/env bash

gem update --system --no-document -q --silent
gem install bundler --no-document -f -q --silent
bundle config mirror.https://rubygems.org ${RUBYGEM_MIRROR}
bundle install
bundle exec ./bin/binary-builder.rb "$@"
